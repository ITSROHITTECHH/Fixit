services:
  - type: web
    name: fixit_backend
    env: python
    buildCommand: pip install -r backend/fixit_backend/requirements.txt
    startCommand: gunicorn backend.fixit_backend.fixit_backend.wsgi:application
    rootDir: backend/fixit_backend
